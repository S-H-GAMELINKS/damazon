<% @cart.cart_items.each do |item| %>
    <%= item.item.name %>
    <%= item.item.price %>
    <%= item.item.description %>
    <%= item.quantity %>
<% end %>

<%= link_to 'Ordered!', cart_path(@cart), method: :put %>